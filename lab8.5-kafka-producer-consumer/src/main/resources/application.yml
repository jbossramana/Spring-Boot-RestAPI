spring:
  kafka:
    consumer:
      auto-offset-reset: earliest
      group-id:  testgroup
      bootstrap-servers:
      - localhost:9092
    producer:
      bootstrap-servers:
      - localhost:9092
    